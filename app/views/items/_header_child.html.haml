%li.header_category__child
  = link_to "#{category.name}", category_path(id: category.id)
  %ul.header__category__grand-children
    = render partial: "items/header_grand-child", collection: category.children, as: "children"